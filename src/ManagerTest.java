public class ManagerTest {
   public static void main(String[] args){

/*
        for (var v:products)
            System.out.println(v.getProductId()+","+v.getAmount()+","+v.getDescription());

        Product proB=new Product();
        proB.setProductId(2);
        proB.setName("ProductB");
        proB.setAmount(2);
        proB.setDescription("This is second product");
        manager.addObj(proB);

        Product productA=manager.searchbyName("ProductA");
        manager.addObj(productA);
        System.out.println(productA.getProductId()+","+productA.getAmount()+","+productA.getDescription());

        Product productB=manager.searchbyName("ProductB");
        System.out.println(manager.toString());

       try {
           SwingUtilities.invokeAndWait(new Runnable() {
               @Override
               public void run() {
                   AddRemoveProduct addRemoveProduct = new AddRemoveProduct();
               }
           });
       } catch (InterruptedException e) {
           e.printStackTrace();
       } catch (InvocationTargetException e) {
           e.printStackTrace();
       }

*/
   }
}
